{
  "name": "reviewglass",
  "version": "0.1.0",
  "phase": 4,
  "phaseName": "P0-P2 and P4 done, P3 built · P4b: the whole file, the latest edit highlighted and a New view · Follow chooses the session under the glass (adr.rg.022) · P6 novice mode built and observed (adr.rg.023), local or remote with the user's own key · spec v0.3",
  "session": 6,
  "updated": "2026-09-23",
  "state": "Session 6 closed 23.9.2026 (main after #32). Shipped: the latest edit in a colour of its own and the New view (observed); Follow choosing the session by the Code pane's header title or a session window's page name through UI Automation (adr.rg.022, observed with the cursor); the startup crash reproduced 5 of 5 with the new panic log (~/.reviewglass/panic.log) and fixed by managing all state on the builder; spec v0.3 (Atlas REVIEWGLASS-SPEC v3 = docs/REVIEWGLASS-SPEC.md) and the model's modules in step; P6 (adr.rg.023): Settings > Explain (Off/Local/Remote, the key in Windows Credential Manager), an Explain button per hunk, the explanation with its backend named and Cancel, the first remote request's disclosure, observed over CDP with a loopback test server and a throwaway key (nothing sent), and one fix from it (the declaration is the one the change is in, not git's header context). 130 tests. The owner's settings: explanations Off, no key stored. No local model server on this machine. The shortcut's exe is rebuilt from main and running. Earlier sessions: see docs/CHANGELOG.md.",
  "deploy": {
    "surface": "the owner's own Windows 11 machine only; no hosted component",
    "binary": "src-tauri/target/release/reviewglass.exe of the main checkout, launched from Desktop and Start Menu shortcuts; the build agent pulls and rebuilds it after every merge and verifies on warm launches; 23.9.2026: rebuilt from main at the close of session 6 (P6 and the declaration fix) and launched the owner's way, without remote debugging",
    "collector": "~/.reviewglass/bin/reviewglass-statusline.exe (statusLine) and ~/.reviewglass/bin/reviewglass-hook.exe (PostToolUse), both configured in ~/.claude/settings.json (backups: settings.json.bak-reviewglass-p0, -p4); the spool they write is ~/.reviewglass/spool (sessions/, events/) — never under AppData (adr.rg.019)",
    "installers": "NSIS and MSI both build; not yet used — P7",
    "atlas": "system reviewglass; status via atlas_put_status; MCP worker redeployed manually after each model merge (CI only dry-runs)"
  },
  "workflow": "Code and docs in English, conversation in Finnish. cargo fmt + clippy -D warnings + cargo test + pnpm check before every commit; CI on windows-latest runs the same. Commit straight to main while the repo is single-author (public since 17.9.2026); the Atlas model changes go through a PR on the Atlas repo, then wrangler deploy of the MCP worker. A new decision enters the Atlas model first and is rendered into docs/adr/ by node scripts/adr-from-model.mjs. End a session by updating docs/CHANGELOG.md, docs/BUILD_INFO.json and, if a lesson was learned, docs/LESSONS.md; put the status to Atlas.",
  "next": [
    "The owner's own look (session 7 starts here): P6 with a real backend - a local model (install Ollama and pull a model, then Settings > Explain > Local, port 11434) or their own API key (Settings > Explain > Remote; the first explanation shows its payload and waits for a yes); and Follow choosing the session (the glass in Follow over the Code panes and the session windows, the switch on the Sessions tab). Polish from what is seen",
    "Spec revision (v0.3 -> v0.4, the repo copy and the Atlas artifact together): the explain-backend contract of adr.rg.023 (the two protocols, 60 s instead of the proposed 30 s, real cancellation, the key checked before the disclosure), the declaration taken from the working copy, P6 built and observed in section 7",
    "The hawk eye (P6b) as a decision first: a watcher through the P6 backend reading the new code as it is written and raising what looks wrong - continuous, opt-in, per repository, the denylist, the payload disclosed, a cost bound; then its design with the owner; then build",
    "Housekeeping: remove the Follow recorder (measure.rs, Settings > Measurements) now that reading is calm; the dock's Still as a decision; the glass's bar thinner; how 'sdk-cli' sessions are shown",
    "P5: cache and PR panels (prompt_cache and pr surfaces, absent-data paths verified)",
    "P7: packaging - the installer places both collectors, gives the toast the app's own identity, and says that a running session keeps the status line it started with",
    "P8: public release after the name check (adr.rg.008): installer and bundle identifier",
    "Foreground-window app profiles and session state in the dock ('needs you'); UI Automation (adr.rg.022) may serve here once measured for this use"
  ],
  "nextUpdated": "2026-09-23"
}
