{
  "name": "reviewglass",
  "version": "0.1.0",
  "phase": 3,
  "phaseName": "P0 spike done · P1 magnifier reads the pane (adr.rg.017) and hangs from the dock (adr.rg.018) · P2 session panel built, exit criterion open · P3 threshold alerts built",
  "session": 2,
  "updated": "2026-09-13",
  "state": "Session 2 closed 13.9.2026 with the owner's verdict 'the direction is good now'. Shipped today, all on main and in the Desktop shortcut's build (stamp on the bar and the dock): adr.rg.017 pane detection with lock, Fit and the viewfinder, reworked twice from tests over real columns and calmed; adr.rg.018 the dock — a corner strip from which the glass is switched on and off, the panel as its drawer, the RG mark as a one-click switch, the global shortcut said and settable in Settings; the glass and the panel start hidden; a CSP hole and a start-up race that blanked the release glass, both found over CDP. Four things observed in use at the end of the day are recorded at the top of docs/CHANGELOG.md and are session 3's starting point: Follow still restless (width changes), the lens should hold still while its menu is open, Still from the dock is black (a bug: freeze without a frame), and the still captures the menu itself. Session 1 (11.-12.9.2026): from spec to a tool the owner uses — P0 spike, P1 magnifier, P2 session panel, P3 alerts, the glass became a window with three modes, a halo, tray and single instance; sixteen ADRs, docs to the family standard.",
  "deploy": {
    "surface": "the owner's own Windows 11 machine only; no hosted component",
    "binary": "src-tauri/target/release/reviewglass.exe of the main checkout, launched from Desktop and Start Menu shortcuts; the build agent pulls and rebuilds it after every merge and verifies on warm launches (the bar and the dock carry the build stamp)",
    "collector": "~/.reviewglass/bin/reviewglass-statusline.exe, configured as statusLine in ~/.claude/settings.json (backup: settings.json.bak-reviewglass-p0)",
    "installers": "NSIS and MSI both build; not yet used — P7",
    "atlas": "system reviewglass; status via atlas_put_status; MCP worker redeployed manually after each model merge (CI only dry-runs)"
  },
  "workflow": "Code and docs in English, conversation in Finnish. cargo fmt + clippy -D warnings + cargo test + pnpm check before every commit; CI on windows-latest runs the same. Commit straight to main while the repo is private and single-author; the Atlas model changes go through a PR on the Atlas repo, then wrangler deploy of the MCP worker. A new decision enters the Atlas model first and is rendered into docs/adr/ by node scripts/adr-from-model.mjs. End a session by updating docs/CHANGELOG.md, docs/BUILD_INFO.json and, if a lesson was learned, docs/LESSONS.md; put the status to Atlas.",
  "next": [
    "Session 3 starts from the four observations at the top of docs/CHANGELOG.md: (1) Follow restless / width changes — one conversation with measurements; (2) the lens holds still while its right-click menu is open; (3) Still from the dock is black — take the freeze_after_publish path; (4) the still captures the menu — freeze the pre-menu frame",
    "The glass's bar: thinner now that activation is the dock's — decide what stays (adr.rg.013 keeps a way off from the glass itself)",
    "Foreground-window app profiles (zoom, size, lock per application) and session state in the dock ('needs you'); UI Automation stays an experiment to be measured",
    "Spec v3: fold adr.rg.012-018 into 6.3 and 6.1 (finder and dock in the topology) — the repo copy and the Atlas artifact together",
    "P2 exit criterion: observe five concurrent sessions in the panel with correct shared-vs-attributed semantics",
    "P4 live diff: hook-collector as a native binary, diff-service, the Diff tab",
    "Toast identity: the unpackaged exe is attributed to Windows PowerShell; the installer (P7) gives the app its own",
    "Memory: five WebView2 windows now — measure and record in spec 8.4",
    "adr.rg.008: clear the name before P8"
  ],
  "nextUpdated": "2026-09-13"
}
